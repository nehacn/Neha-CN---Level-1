//Write a program to add two user input numbers using one function.
#include<stdio.h>
int main()
{
     int a,b,sum;
      printf("Enter 2 numbers\n");
      scanf("%d%d",&a,&b);
      sum=a+b;
      print("\n The result is %d",sum);
      return 0;
     }
